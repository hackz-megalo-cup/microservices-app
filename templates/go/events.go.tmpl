package __SERVICE_NAME_SNAKE__

// ==========================================================.
// Event types — define your domain events here.
// These map directly to your Event Storming sticky notes.
// ==========================================================.

const (
	Event__SERVICE_NAME_PASCAL__Created     = "__SERVICE_NAME_SNAKE__.created"
	Event__SERVICE_NAME_PASCAL__Failed      = "__SERVICE_NAME_SNAKE__.failed"
	Event__SERVICE_NAME_PASCAL__Compensated = "__SERVICE_NAME_SNAKE__.compensated"
)

// ==========================================================.
// Event data — the payload for each event.
// Add fields that capture what happened.
// ==========================================================.

// __SERVICE_NAME_PASCAL__CreatedData is the payload for the created event.
type __SERVICE_NAME_PASCAL__CreatedData struct {
	Input  string `json:"input"`
	Output string `json:"output"`
}

// __SERVICE_NAME_PASCAL__FailedData is the payload for the failed event.
type __SERVICE_NAME_PASCAL__FailedData struct {
	Input string `json:"input"`
	Error string `json:"error"`
}

// __SERVICE_NAME_PASCAL__CompensatedData is the payload for the compensated event.
type __SERVICE_NAME_PASCAL__CompensatedData struct {
	Reason string `json:"reason"`
}
