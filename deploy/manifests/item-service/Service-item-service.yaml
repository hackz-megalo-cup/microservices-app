apiVersion: v1
kind: Service
metadata:
  name: item-service
  namespace: microservices
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/name: item
    app.kubernetes.io/version: 0.1.0
