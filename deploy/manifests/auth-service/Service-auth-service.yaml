apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: microservices
spec:
  ports:
    - name: http
      port: 8090
      protocol: TCP
      targetPort: 8090
  selector:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/version: 0.1.0
