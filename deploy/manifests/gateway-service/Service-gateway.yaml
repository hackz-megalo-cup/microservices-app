apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: microservices
spec:
  ports:
    - name: http
      port: 8082
      protocol: TCP
      targetPort: 8082
  selector:
    app.kubernetes.io/name: gateway
    app.kubernetes.io/version: 0.1.0
