apiVersion: v1
kind: Service
metadata:
  name: caller-service
  namespace: microservices
spec:
  ports:
    - name: http
      port: 8081
      protocol: TCP
      targetPort: 8081
  selector:
    app.kubernetes.io/name: caller
    app.kubernetes.io/version: 0.1.0
